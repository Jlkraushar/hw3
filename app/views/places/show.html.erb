<h1><%= @place["name"] %></h1>

<ul>
  <% for entry in @entries %>
    <li>
      <a href="/entries/<%= entry["id"] %>">
        <%= entry["title"] %>, <%= entry["description"] %>
      </a>
    </li>
  <% end %>
</ul>
